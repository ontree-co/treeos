package config

// Port configuration constants
const (
	// DefaultPort is the default port for the OnTree server
	DefaultPort = ":3000"
	
	// TestPort is the port used for E2E tests to avoid conflicts
	TestPort = ":3001"
)