# Disk Speed Test Configuration
# Copy this file to .env and adjust the values as needed

# Port mapping - change if 8082 is already in use
PORT=8082

# User and Group IDs for proper file permissions
# Run 'id -u' to get your user ID
# Run 'id -g' to get your group ID
PUID=1000
PGID=1000

# Path to test disk speed
# This can be any directory you want to benchmark
# Examples:
#   - ./test-data (local test directory)
#   - /mnt/nas (network storage)
#   - /home/user/documents (user directory)
TEST_PATH=./test-data

# treeOS specific settings (optional)
APP_SUBDOMAIN=diskspeed