# Cooking Companion Environment Variables
# Copy this file to .env and adjust values as needed.

# Database configuration
DB_USER=cookingcompanion
DB_PASSWORD=test-password-change-me
DB_NAME=cookingcompanion

# Application settings
# Host port mapping (container listens on 8080)
HOST_PORT=8085
# IMAGE_REPOSITORY=ghcr.io/ontree-co/cookingcompanion
# VERSION=latest

# Optional: customize the subdomain shown in the dashboard
# APP_SUBDOMAIN=cooking-companion
