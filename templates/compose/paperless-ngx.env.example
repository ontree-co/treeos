# Paperless NGX Environment Variables
# This file should be copied to .env and customized

# TreeOS required variables (do not change)
COMPOSE_PROJECT_NAME=ontree-paperless-ngx
COMPOSE_SEPARATOR=-

# REQUIRED: Security key for Django (generate a long random string)
# You can generate one with: openssl rand -base64 32
PAPERLESS_SECRET_KEY=change_me_to_a_very_long_random_string_at_least_50_characters

# Application settings
PAPERLESS_PORT=8010

# Timezone (see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
PAPERLESS_TIME_ZONE=Europe/Berlin

# Primary OCR language
# Common values: eng (English), deu (German), fra (French), spa (Spanish)
PAPERLESS_OCR_LANGUAGE=eng

# Optional: Additional OCR languages (space-separated)
# Example: "deu fra spa"
# See available languages: https://packages.debian.org/search?keywords=tesseract-ocr-&searchon=names
# PAPERLESS_OCR_LANGUAGES=

# Optional: Public URL (required if using reverse proxy or exposing publicly)
# PAPERLESS_URL=https://paperless.yourdomain.com

# Admin user creation happens automatically on first access
# When you first visit Paperless NGX, you'll be prompted to create your admin account

# Optional: User/Group mapping for file permissions
# Set these to your host user's UID/GID for proper file access
# Find your UID/GID with: id -u and id -g
# USERMAP_UID=1000
# USERMAP_GID=1000

# Optional: Auto-login username (for single-user setups, not recommended for multi-user)
# PAPERLESS_AUTO_LOGIN_USERNAME=

# Optional: Document filename format when exporting
# Variables: {correspondent}, {document_type}, {tag_list}, {title}, {created_year}, {created_month}, {created_day}
# PAPERLESS_FILENAME_FORMAT={created_year}/{correspondent}/{title}

# Optional: Email settings for importing documents via email
# PAPERLESS_EMAIL_HOST=smtp.gmail.com
# PAPERLESS_EMAIL_PORT=587
# PAPERLESS_EMAIL_HOST_USER=your-email@gmail.com
# PAPERLESS_EMAIL_HOST_PASSWORD=your-app-password
# PAPERLESS_EMAIL_USE_SSL=false
# PAPERLESS_EMAIL_USE_TLS=true

# Optional: Advanced settings
# PAPERLESS_CONSUMER_POLLING=60
# PAPERLESS_CONSUMER_DELETE_DUPLICATES=true
# PAPERLESS_CONSUMER_RECURSIVE=true
# PAPERLESS_CONSUMER_SUBDIRS_AS_TAGS=false